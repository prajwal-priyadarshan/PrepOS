node_modules
dist
dist-ssr
*.local

# Rust build output is redirected to a local-only path by .cargo/config.toml,
# but keep this in case that file is removed on another machine.
src-tauri/target
src-tauri/gen

.DS_Store
*.log
